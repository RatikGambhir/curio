{
	"name": "curio-processor-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --persist-to ../.wrangler-state",
		"start": "wrangler dev --persist-to ../.wrangler-state",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.20.3",
		"typescript": "^7.0.2",
		"vitest": "^4.1.10",
		"wrangler": "^4.120.0"
	},
	"dependencies": {
		"@google/genai": "^2.16.0"
	}
}
